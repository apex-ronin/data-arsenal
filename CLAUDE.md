## Data Arsenal Workspace Restrictions

1. **Trust But Verify (Pre-Flight Audit)**: Before executing a single line of code, you must read `data_arsenal_roadmap.md` and conduct a rigorous code, security, and architecture audit based on the technological landscape of April 2026. If you detect any hallucinations, deprecated Vertex AI logic, or CMMC GovTech compliance gaps, you must **PAUSE** and propose a superior fix to the User. If the roadmap is sound, you must explicitly output: *"I have audited this roadmap, I 100% agree with its technical validity, and I accept full responsibility for its execution"* before proceeding. You are the last line of defense.
2. **Execute Locally**: You are restricted to running and writing scripts entirely within this `data-arsenal/` workspace. Do not bridge files directly to `ronin/`.
3. **Sovereign-local architecture (no cloud)**: GCP was torn down 2026-06-06. The stack is sovereign-local — retrieval runs on local **FAISS** indexes at `G:\AI-Models\indexes\` (nomic-embed-text-v1.5 served by LM Studio at `localhost:1234`), governed by `index_manifest.json`. Do NOT reintroduce Vertex AI, Vertex Vector Search, GCS buckets, Cloud Logging, Cloud Run, or any `google-cloud-*` / `vertexai` / `google.adk` dependency — all of that is archived under `_archive_gcp/`. The legacy GCP/Vertex code is reference-only. (Historical note: `us-east5` vs `us-central1` region debate is moot — there is no cloud region anymore; CMMC L2, DFARS 252.239-7010, and FedRAMP Moderate impose no specific region requirement, confirmed 2026-05-14.)
4. **Commander's Intent + Semantic Hard-Stops**: Do not attempt continuous execution on autopilot or rely solely on local passes/tests. Tests validate observable behavior, not strings. If you hit a Semantic Hard-Stop or an undocumented edge case, STOP and ask the user.
5. **Trust but Verify**: No substantive commit ships without peer review.
6. **Regulatory Citations**: Any embedded policy logic (like M-26-04) must include the primary source URL inline as a comment.
7. **Session Closing**: Before terminating, you must update the **global** `STATE.md` at `C:\Users\jnel9\OneDrive\Workspaces\STATE.md` (canonical single source of truth — no copies in sub-workspaces), check off tasks in `data_arsenal_roadmap.md`, and commit the current state after getting peer review.
